machine:
  node:
    version: 5.6.0

dependencies:
  pre:
    - nvm use v5.6.0 && nvm alias default v5.6.0

deployment:
  production:
    branch: "master"
    commands:
      - printf "${METEOR_USERNAME}\n${METEOR_PASSWORD}\n" | meteor login
      - meteor publish
