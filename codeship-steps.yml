- service: app
  name: ci
  command: npm run test
- service: app
  name: meteor login
  command: ./meteor-login.exp ${METEOR_USERNAME} ${METEOR_PASSWORD}
  tag: master
- service: app
  name: meteor publish
  command: meteor publish
  tag: master
